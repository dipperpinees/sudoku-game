#ifndef __HEADER_
#define __HEADER_

#include <SDL.h>
#include "LTexture.h"
#include <SDL_ttf.h>
#include <SDL_mixer.h>
#include <vector>

string to_String(int i);
void loadMedia();
void close();
void playGame();

#endif // __HEADER_
